program list

head(Cons(x, xs)) = x
tail(Cons(x, xs)) = xs

ignoreSecond(x, xs) = x
ignoreFirst(x, xs) = xs

zeros() = Cons(Z(), zeros())

plus1(x) = S(x)

infiniteListOf(x) = Cons(x, infiniteListOf(x))

main = tail(Cons(x, Cons(Z(), Nil())))
//ignoreFirst(zeros(), plus1(Z()))
//head(tail(infiniteListOf(plus1(Z()))))
