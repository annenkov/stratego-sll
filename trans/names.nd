module names

namespaces
    program
    f-function
    g-function

rules
  Program(name, _): defines program name
    FDef(fname, agrs*, body): defines unique f-function fname
    FCall(fname, args*): refers to f-function fname
